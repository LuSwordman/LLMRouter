# 🧠 LLMRouter — 多模型智能路由系统

一个基于 **LangChain4j + OpenAI 接口兼容模型** 的多大语言模型路由与容错系统，支持轮询、权重、故障转移三种策略，并结合本地语义匹配与 LLM 判断实现精准房型名称匹配。

---

## 🚀 特性 Highlights

- 🤖 **多模型接入**：支持 OpenAI、DeepSeek、智谱 GLM 等任意兼容 OpenAI 接口的模型
- ⚙️ **多策略路由**：
    - `ROUND_ROBIN` — 轮询分发
    - `WEIGHTED` — 权重调度
    - `FAILOVER` — 故障自动切换
- 🧩 **异步与并发支持**：内置线程池，可批量并发执行数十任务
- 💡 **容错机制**：调用失败自动降级到下一个模型
- 🏨 **房型匹配引擎**：
    - 支持本地相似度算法（余弦 + 编辑距离）
    - 支持大模型语义决策 (`llmPickBestRoom`)
- 📈 **批次并发测试**：可快速验证吞吐与稳定性

---

## 🧱 系统架构

```mermaid
graph LR
A[Client] --> B[LLMRouter]
B --> C1[OpenAI]
B --> C2[DeepSeek]
B --> C3[GLM]
B --> C4[其他模型...]
B --> D[Failover / Weighted / Round-Robin]
